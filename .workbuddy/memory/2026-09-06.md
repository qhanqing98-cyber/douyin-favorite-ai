# 2026-09-06

- 抖音收藏夹本地应用（douyin-favorite-ai）：完成技术方案分析，产出 `技术方案.md`。
- 选型：Playwright persistent context 登录 + response 拦截采集（不逆向签名）、SQLite + FTS5 trigram 中文搜索、faster-whisper 本地转写、混元 API 概要/问答、FastAPI 本地 UI。
- 里程碑 M0-M5 已定，尚未开始写代码；下一步是 M0 环境搭建与 M1 登录采集。
- 方案补充（用户确认的设计）：默认不下载视频，只存元数据；转写按需勾选 + 批量补齐，只下音频转完即删；转写文本进 FTS5 索引参与召回并支持问答引用；跳转键用 aweme_id 拼视频页 URL，标题仅作展示。
- M0+M1 代码完成：app/db.py（FTS5 trigram + 触发器同步 + 短查询 LIKE 降级）、app/crawler.py（persistent context 登录、response 拦截、滚轮加载、3 轮无增长即停）、main.py CLI（login/crawl/search/stats）。venv 位于 C:\Users\丘汉清\.workbuddy\binaries\python\envs\douyin-fav。
- 冒烟测试通过：插入/去重/中文长查询 FTS/2 字查询 LIKE 降级均正确；测试数据已清理。
- 已知坑：FTS5 trigram 无法匹配 <3 字符查询，必须 LIKE 降级。chromium 下载仍在后台进行。
- 下一步：用户扫码 login → crawl 真实采集验收（条数比对收藏夹）。
- 环境迁移：venv 移到项目内 `D:\桌面\douyin-favorite-ai\.venv`（用户要求）。官方 CDN 下载 Chromium 卡死（12 分钟停在 20%），改用 npmmirror 镜像 `PLAYWRIGHT_DOWNLOAD_HOST=https://cdn.npmmirror.com/binaries/playwright` 后 55 秒完成，Chromium 151.0.7922.34 启动验证通过。后续国内下载 playwright 浏览器一律走该镜像。
- 残留：`C:\Users\丘汉清\.workbuddy\binaries\python\envs\douyin-fav` 旧 venv 删除失败（genie-trash 路径拼接 bug，fail-closed），约 40MB，可手动删。
- 扫码登录成功（轮询 sessionid cookie 方案）。首爬 120s 无数据：诊断发现 (1) 收藏接口为 /aweme/v1/web/aweme/favorite/；(2) 直接打开 ?showTab=favorite_collection 不触发请求，必须 goto user/self 后模拟点击"收藏"tab。已修复 crawler 并重爬。
- 通用经验：抖音 SPA 直达 URL 常不触发数据请求，采集类脚本要"落地页 + 模拟点击"而非依赖 URL 参数。
- 用户提供了两个同类项目参考：Jonesxq/douyin_RAG（Playwright 扫码 + 收藏夹接口链路 + ChromaDB + BGE-small-zh-v1.5 + faster-whisper + Qwen）、lys2-14/douyin-knowledge-rag（手动 Cookie + yt-dlp + DeepSeek + MMR 检索，版权视频退化用标题+描述）。结论：二期语义检索直接用 bge-small-zh-v1.5 + 向量库；补"下载失败退化用标题+描述"策略。
- crawl 点击加固：改为"点击 → 20s 等数据 → 重试最多 3 次"循环，手动兜底 90s。首爬失败根因是 sleep 4s 点击过早（诊断脚本 sleep 5-6s 均成功触发接口，接口返回 200 + aweme_list 正常）。
- 滚动翻页排障（3 轮诊断定位）：(1) window 不可滚动，滚动条在 .route-scroll-container（语义类名，稳定）；(2) mouse.wheel 不生效，须 JS el.scrollTop += 2000；(3) 最关键：首页接口 /aweme/v1/web/aweme/favorite/，翻页起改用 /aweme/v1/web/aweme/listcollection/——过滤器必须同时含两个特征。
- 最终结果：460 条收藏全部入库（0 空标题、460 有 share_url/play_url、423 有标签），重跑去重正确，has_more=0 收尾。中文搜索验收通过（"王者荣耀"命中）。
- 阶段一完成。阶段二待做：yt-dlp 取音频 → faster-whisper 转写 → bge-small-zh 向量 + 混元概要/问答；补"下载失败退化用标题+描述"策略。
- 阶段二核心链路完成并验证：db 迁移（transcript/summary 列 + FTS 重建含 transcript）、crawler.export_cookies()（Netscape 格式，域含点才写 TRUE——yt-dlp 会拒收非点开头的 TRUE 行）、transcribe.py（yt-dlp -m 调用、faster-whisper CPU int8 约 1.7x 实时）、llm.py（混元 OpenAI 兼容，读 .env）。
- 模型下载三连坑：(1) HF Xet 协议经 hf-mirror 返回 0 字节 model.bin 且 hub 报成功 → 解法：curl 直接拉镜像 resolve 文件到 models/faster-whisper-small/，WhisperModel 传本地路径；(2) HF_HUB_DISABLE_XET=1；(3) 仓库本无 preprocessor_config.json，"Entry not found" 是正常响应。
- 中文关键坑：Whisper 中文输出混繁体，trigram 无字形归一化，必须 OpenCC t2s 转简体再入库（否则"人性規律"搜不到"规律"）。
- 转写实测：2 条，1867/3389 字，CPU int8 small 约 1.7x 实时速度。转写内容搜索验证通过。
- 剩余：summarize/ask 命令已写好，等用户在 .env 配 HUNYUAN_API_KEY 后验收。
- 阶段二收尾：LLM 切换 DeepSeek（.env: LLM_API_KEY/LLM_BASE_URL=https://api.deepseek.com/v1/LLM_MODEL=deepseek-chat），llm.py 改为通用 OpenAI 兼容配置（默认 DeepSeek），.env.example 给出混元/Ollama 切换示例。
- DeepSeek 端到端验收通过：summarize 2 条完成；ask "RAG面试会问哪些问题" 返回带 [来源N] 标注的完整回答。
- 修复两个真实 bug：(1) db.search 的 FTS/LIKE 两条路径 SELECT 均漏 transcript 列，cmd_ask 全部结果被误过滤；(2) 整句问题当单一 FTS 短语几乎必然零命中——引入 jieba 分词，长查询取 ≥2 字词 OR 连接 + bm25 排序，零命中再 LIKE 兜底。
- 批量转写 20 条已在后台启动（task 3LAJZg）。
- 批量转写 20 条结果：11 条成功，9 条 yt-dlp 报"Fresh cookies needed"（实为间歇性 403 限流，单发重试即成功）。修复：_download_audio 加 3 次随机退避重试 + 条目间 2-5s 随机间隔；兜底行重置为 NULL 后重跑（后台 task syaUCM）。
- DeepSeek 概要覆盖 13 条（含首批 2 条），ask/summarize/search 三链路全部实测通过。
- 当前库内状态：460 条元数据，有效转写持续增长中，概要随转写跟进生成。
- 重跑 10/10 全部成功（8 条限流失败的恢复 + 2 条新增），重试+间隔修复确认有效。库内：有效转写 24/460，概要 23 条。
- 待用户决定是否挂全量 transcribe --all（预计 15-25h CPU）。
- M5 Web UI 完成：app/web.py（FastAPI：/api/stats、/api/videos 搜索+列表、/api/ask、/api/transcribe、/api/summarize、/api/job，后台线程单任务+轮询）+ static/index.html（深色单页：搜索/问答/收藏库/任务按钮）。main.py 加 web 子命令（默认端口 8642）。db 加 list_videos、stats 扩展。冒烟：全部端点 200/正常 JSON。
- 用户选择暂不全量转写，先用现有 24 条转写。
- Web UI 增强：一键转写全部/一键概要全部（JobReq 加 all 标志，transcribe.run 加 progress 回调，db 加 count_untranscribed/count_unsummarized），任务区加进度条（done/total 百分比）。.gitignore 补 .venv/ 和 models/。实测一键概要任务跑通。
