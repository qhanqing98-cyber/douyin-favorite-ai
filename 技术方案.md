# 抖音收藏夹本地应用 — 技术方案

## 一、需求拆解

| 阶段 | 需求 | 核心难点 |
|------|------|---------|
| 一 | 登录抖音，爬取个人收藏夹（标题/标签/来源/网址），支持关键词模糊召回 | 反爬签名、中文全文搜索 |
| 二 | 批量语音转文字 → AI 生成概要 → 可对单个视频提问 | 音频获取、本地 ASR、上下文问答 |

## 二、技术选型

| 环节 | 选型 | 理由 |
|------|------|------|
| 语言 | Python 3.12+ | Playwright 生态最全，已有经验 |
| 登录与抓取 | Playwright **persistent context**（有头） | 首次扫码后登录态落在 `user_data_dir`，之后免登录 |
| 采集方式 | 页面滚动 + `page.on("response")` 拦截收藏接口 JSON | 不逆向 X-Bogus/a_bogus 签名（易失效、有风控），签名由页面自己生成，只捡现成响应 |
| 存储 | SQLite 单文件 | 零运维、单机够用 |
| 关键词搜索 | SQLite FTS5（`tokenize='trigram'`） | trigram 支持中文子串匹配，不需要分词器；默认 unicode61 切不开中文 |
| 语义召回（可选增强） | sqlite-vec + bge-small-zh ONNX 嵌入 | "忘记关键词"场景：按意思搜而非按字搜 |
| 音频获取 | yt-dlp（支持抖音）或 play_addr 直链 + cookies → ffmpeg 抽音轨 | 直链有时效，必须即时带登录态下载 |
| 语音转文字 | faster-whisper（medium，int8，CPU 可跑） | 比官方 whisper 快 3-4 倍，中文效果够用 |
| AI 概要/问答 | 腾讯云混元 API（已有 key）；备选 Ollama 本地 | 复用现有账号体系 |
| UI | FastAPI + 单页 HTML（127.0.0.1 本机） | 比 Electron/Tauri 轻一个量级；后期想要桌面壳再包 |

## 三、关键风险与对策

1. **反爬风控**：只爬自己的收藏夹，行为上模拟真人（滚动间隔 2-4s 随机、不并发、单次会话控制条数）。风险极低但不为零。
2. **抖音改版**：前端接口路径和字段会变。解析逻辑单独放 `crawler.py`，全部用 `dict.get()` 容错取值，坏了只改一个文件。
3. **FTS5 trigram 依赖 SQLite ≥ 3.34**：先跑 `python -c "import sqlite3; print(sqlite3.sqlite_version)"` 确认，低了换 trigram 降级方案 LIKE。
4. **视频直链时效**：拦截到的 play_url 可能几分钟内过期，转写流水线里"取链接→下载"必须串行即时执行。
5. **长视频转写成本**：按 aweme_id 幂等缓存，转过的不再转。

## 四、数据流

```
扫码登录 → 登录态存 user_data_dir
    ↓
打开收藏夹页，脚本模拟滚动 → 前端自己带签名请求 favorite/list
    ↓
response 事件捕获 JSON → aweme_list[]
    ↓
逐条提取：desc(标题) / text_extra(标签) / author.nickname(来源)
         / share_url(网址) / aweme_id(主键)
    ↓
INSERT OR IGNORE INTO favorites（增量，重跑不重复）
    ↓
搜索：query → FTS5 MATCH（标题/标签/转写文本，+向量余弦相似）→ 召回列表（附命中片段高亮）
    ↓
用户勾选 → 即时取直链下载 mp4 → ffmpeg 抽 wav
    ↓
faster-whisper → segments 拼接 transcript（存回该行，并同步进 FTS 索引）
    ↓
混元 API → summary（缓存入库）
    ↓
提问：召回相关转写片段作为上下文 → 混元"看着原文"回答
    ↓
回答末尾附来源列表：标题（人眼确认）+ aweme_id 跳转链接（机器定位）
```

## 五、下载与转写策略

- 阶段一**不下载任何视频**，只存元数据；全库只有文本。
- 阶段二下载的是**音频不是视频**：取直链下载 mp4 → ffmpeg 抽音轨 → faster-whisper 转写 → 音频文件可删（留 transcript 即可）。
- 默认**按需转写**：搜索结果中勾选哪条转哪条；支持批量补齐模式（`python main.py transcribe --all`）。
- 幂等：`transcript` 字段非空的自动跳过，重跑不重复。
- 二次检索键：对外展示标题，跳转用 `aweme_id` 拼 `https://www.douyin.com/video/{aweme_id}`（标题可被作者修改，aweme_id 永久唯一）。

## 六、数据库 Schema

```sql
CREATE TABLE favorites (
    aweme_id   TEXT PRIMARY KEY,
    title      TEXT,
    tags       TEXT,          -- JSON 数组
    author     TEXT,          -- 来源（作者昵称）
    share_url  TEXT,
    play_url   TEXT,
    fav_time   INTEGER,       -- 收藏时间戳
    transcript TEXT,          -- 阶段二
    summary    TEXT           -- 阶段二
);
CREATE VIRTUAL TABLE favorites_fts USING fts5(
    title, tags, transcript, content='favorites', content_rowid='rowid',
    tokenize='trigram'
);
```

## 七、目录结构

```
douyin-favorite-ai/
├── app/
│   ├── auth.py        # 启动有头浏览器，首次扫码，保存登录态
│   ├── crawler.py     # 滚动 + response 拦截 + 字段解析
│   ├── db.py          # 建表、upsert、查询
│   ├── search.py      # FTS5 搜索（+向量召回扩展位）
│   ├── transcribe.py  # 下载音频 + faster-whisper
│   ├── llm.py         # 混元封装：概要 / 问答
│   └── web.py         # FastAPI：搜索页、详情页、问答接口
├── browser_data/      # 登录态（gitignore）
├── audio_cache/       # 音频临时文件（gitignore）
├── data/favorites.db
├── .env               # HUNYUAN_API_KEY（gitignore）
└── main.py            # CLI 入口：login / crawl / search / transcribe / serve
```

## 八、实现路线（里程碑）

| 里程碑 | 内容 | 预估 |
|--------|------|------|
| M0 | venv + playwright + ffmpeg 环境就绪 | 0.5 天 |
| M1 | 登录 + 滚动采集 + 解析入库（增量） | 1-2 天 |
| M2 | FTS5 全文搜索 + 命令行/网页搜索页 | 1 天 |
| M3 | 音频批量下载 + whisper 转写入库 | 2-3 天 |
| M4 | 混元概要生成 + 单视频问答 | 2 天 |
| M5 | FastAPI 页面整合（搜索/详情/问答） | 1-2 天 |

M1 完成即产生实际价值（收藏可搜索），M3 是工作量最大的一块，建议先挑 5-10 条验证全链路再放量。

## 九、验证方式

- M1 验收：`python main.py crawl` 后 `SELECT COUNT(*)` 与收藏夹条数一致，重跑数量不变。
- M2 验收：中文子串（如只记得"操作系统"三个字）能召回对应视频。
- M3 验收：抽 3 条视频，人工比对转写与口播内容。
- M4 验收：对转写文本提问，回答引用了视频中的具体内容。
